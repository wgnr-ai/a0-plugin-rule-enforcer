name: rule_enforcer
title: Rule Enforcer
description: Enforces configurable behavioral rules at the tool execution layer. Blocks tool calls that violate rules and returns corrective guidance to the agent.
version: 1.0.0
settings_sections: [developer]
per_project_config: false
per_agent_config: false
always_enabled: false
